import Spinner from "@/components/loadingui/Spinner";

export default function Loading() {
    return (
        <div>
            <h1>user loading </h1>
        <Spinner />
        </div>
    );
    }